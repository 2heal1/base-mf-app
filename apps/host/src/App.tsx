import React, { useEffect } from "react";
import { Router } from "./routing/Router";

export const App = () => {
  // Get auth object from the AuthProvider

  // Set new object when newAuth object changes
  return <Router />;
};
